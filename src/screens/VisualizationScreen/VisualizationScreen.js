import React from 'react';
import {VisualizationContainer} from '../../containers';

const VisualizationScreen = ({route}) => (
  <VisualizationContainer route={route} />
);

export default VisualizationScreen;
