import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  cellTitle: {
    fontSize: 11,
    fontWeight: 'bold',
  },
  cellValue: {
    fontSize: 11,
  },
});

export default styles;
